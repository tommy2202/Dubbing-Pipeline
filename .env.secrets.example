## Secrets for local dev / self-hosted production (never commit `.env.secrets`)
#
# Copy this file to `.env.secrets` and fill values.
# DO NOT commit `.env.secrets`.
#
# Generate strong values (example):
#   python3 -c "import os,base64; print(base64.b64encode(os.urandom(32)).decode())"

# --- auth secrets (required for production; dev defaults exist but are insecure) ---
JWT_SECRET=change-me
SESSION_SECRET=change-me
CSRF_SECRET=change-me

# Legacy/simple API token (avoid using on public networks; legacy token-in-URL is OFF by default)
API_TOKEN=change-me

# Optional admin bootstrap (created/updated at server startup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# Optional HuggingFace tokens (for gated models)
# HUGGINGFACE_TOKEN=change-me
# HF_TOKEN=change-me

# Optional Redis (may include credentials)
# REDIS_URL=redis://:password@redis:6379/0

# Character store encryption (optional but recommended if using character persistence)
# CHAR_STORE_KEY=change-me

# Encryption at rest for sensitive artifacts (optional; if enabled, ARTIFACTS_KEY is required)
# ARTIFACTS_KEY=change-me
# ARTIFACTS_KEY_FILE=/path/to/artifacts.key

# Notifications (optional; private self-hosted ntfy)
# NTFY_AUTH=token:change-me

# WebRTC TURN (optional; keep private)
# TURN_URL=turn:turn.example.com:3478
# TURN_USERNAME=change-me
# TURN_PASSWORD=change-me

# Optional backup destination
# BACKUP_S3_URL=s3://your-bucket/dubbing-pipeline-backups/

