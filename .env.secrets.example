## Secrets for local development (do not commit `.env.secrets`)
##
## You can generate strong values like:
##   python -c "import os,base64; print(base64.b64encode(os.urandom(32)).decode())"

# Auth secrets (recommended to set in production; tests/dev may use defaults)
JWT_SECRET=change-me
CSRF_SECRET=change-me
SESSION_SECRET=change-me

# Optional admin bootstrap
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=change-me

# Optional tokens
# HUGGINGFACE_TOKEN=change-me

# Optional Redis (may include credentials)
# REDIS_URL=redis://:password@redis:6379/0

# Optional WebRTC TURN
# TURN_URL=turn:turn.example.com:3478
# TURN_USERNAME=change-me
# TURN_PASSWORD=change-me

# Optional backup destination
# BACKUP_S3_URL=s3://your-bucket/anime-v2-backups/

