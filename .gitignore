.env
.env.secrets

# local secret files
secrets/
*.key
*.pem

# python caches
__pycache__/
*.pyc
*.pyo
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
.venv/
venv/
.coverage
coverage.xml
htmlcov/

# runtime artifacts
logs/
*.log
Output/**
Input/**
uploads/
outputs/
_tmp*/
_tmp_*/
tmp/
*.db
*.sqlite
*.sqlite3
Input/uploads/
data/reports/
voices/embeddings/

# ntfy local/private config (do not commit)
docker/ntfy/server.yml

# packaging/build output (generated)
build/
dist/
releases/

# local backups (never commit)
backups/

# keep placeholders (runtime dirs must exist but stay empty in git)
!Input/.gitkeep
!Output/.gitkeep

# optional runtime state dir if configured under repo root
data/runtime/
