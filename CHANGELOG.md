# Changelog

This project is now published and documented as **Dubbing Pipeline**.

## 2026-01-13

- Renamed the primary Python package to `dubbing_pipeline`.
- Renamed the legacy package to `dubbing_pipeline_legacy`.
- Renamed CLI entrypoints to `dubbing-pipeline` / `dubbing-web` (with short aliases `dub` / `dub-web`).
- Removed historic naming that referenced earlier internal version labels.

